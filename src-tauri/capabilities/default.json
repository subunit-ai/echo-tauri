{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window + orb overlay",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
